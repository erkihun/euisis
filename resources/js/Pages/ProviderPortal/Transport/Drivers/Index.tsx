import TransportProviderLayout from '@/Layouts/TransportProviderLayout';
import { Link } from '@inertiajs/react';
export default function Index({ drivers = [] }: { drivers: any[] }) { return <TransportProviderLayout title="Drivers"><div className="mb-4"><Link href={route('provider.portal.transport.drivers.create')} className="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">New Driver</Link></div><div className="rounded-lg border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900">{drivers.map((item) => <Link key={item.id} href={route('provider.portal.transport.drivers.edit', item.id)} className="block border-b border-slate-100 px-4 py-3 text-sm last:border-0 dark:border-slate-800">{item.full_name}</Link>)}</div></TransportProviderLayout>; }
