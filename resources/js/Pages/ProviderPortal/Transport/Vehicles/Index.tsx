import TransportProviderLayout from '@/Layouts/TransportProviderLayout';
import { Link } from '@inertiajs/react';
export default function Index({ vehicles = [] }: { vehicles: any[] }) { return <TransportProviderLayout title="Vehicles"><div className="mb-4"><Link href={route('provider.portal.transport.vehicles.create')} className="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">New Vehicle</Link></div><div className="rounded-lg border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900">{vehicles.map((item) => <Link key={item.id} href={route('provider.portal.transport.vehicles.edit', item.id)} className="block border-b border-slate-100 px-4 py-3 text-sm last:border-0 dark:border-slate-800">{item.vehicle_code} - {item.plate_number}</Link>)}</div></TransportProviderLayout>; }
