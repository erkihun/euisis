import TransportProviderLayout from '@/Layouts/TransportProviderLayout';
import { Link } from '@inertiajs/react';
export default function Index({ trips = [] }: { trips: any[] }) { return <TransportProviderLayout title="Trips"><div className="mb-4"><Link href={route('provider.portal.transport.trips.create')} className="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">New Trip</Link></div><div className="rounded-lg border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900">{trips.map((item) => <Link key={item.id} href={route('provider.portal.transport.trips.edit', item.id)} className="block border-b border-slate-100 px-4 py-3 text-sm last:border-0 dark:border-slate-800">{item.trip_number} - {item.route?.name_en}</Link>)}</div></TransportProviderLayout>; }
